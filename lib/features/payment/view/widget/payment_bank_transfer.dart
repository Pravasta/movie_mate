import 'package:flutter/material.dart';

class PaymentBankTransfer extends StatelessWidget {
  const PaymentBankTransfer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
